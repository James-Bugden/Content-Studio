# M3 prompt — Unified QA, cutover and retirement

Run the combined verification campaign, fix findings, deploy one Content Studio
candidate and smoke-test the exact build. Update both repositories' issue/docs
state so the standalone Social Replies deployment and historical-import gates are
explicitly superseded. Preserve rollback and do not delete production data.

