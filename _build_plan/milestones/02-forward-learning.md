# M2 prompt — Forward-only learning and Content Idea bridge

Make recorded replies the only required source of future personal reply history.
Remove historical export/private evaluation from release gates and user-facing
setup. Add an explicit, authenticated and idempotent Save as Content Idea action
that writes through Content Studio's Google Sheet repository into the existing
idea intake without scheduling, Typefully handoff or silent promotion.

