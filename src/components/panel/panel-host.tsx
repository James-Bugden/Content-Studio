'use client';

/**
 * Side-panel host (UX redesign). Mounted once in the studio layout; opens when
 * the URL carries `?post=` or `?slot=`. Filled in by panel-host implementation.
 */
export function PanelHost() {
  return null;
}
